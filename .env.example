# AcentOS no necesita variables de entorno.
# Toda la persistencia es 100 % localStorage y no hay backend ni claves de API.
# Este archivo se conserva como marcador por si en el futuro se agrega configuración.
